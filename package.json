{
  "name": "mad-vervet-hostel",
  "version": "1.0.0",
  "private": true,
  "description": "Mad Vervet Hostel — static site + Netlify Functions backend (bookings, Stripe prepay, inquiries)",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "resend": "^4.0.1",
    "stripe": "^17.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
